# Chalo Golo

Goal-first learning app (React + Vite). The UI and product name are **Chalo Golo** ("walk toward your goals").

## Run locally

```bash
cd frontend
npm install
npm run dev
```

See `frontend/README.md` for more detail.
